    return images, labels  -c "from data.utils import load_cifar10, preprocess_data; (x_train, y_train), (x_test, y_test) = load_cifar10(); x_train, y_train = preprocess_data(x_train, y_train); print('SUCCESS: Data works!')"
